package constants

const (
	// REMOTE_WORKER_NAME is actor name for worker
	REMOTE_WORKER_NAME = "worker"

	// REMOTE_COORDINATOR_NAME is actor name for coordinator
	REMOTE_COORDINATOR_NAME = "coordinator"
)
